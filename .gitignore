.DS_Store
*.log
.env
__pycache__/
*.pyc
dashboard/data/*.db
output/documents/
output/job-radar/debug_job_log.txt
output/job-radar/*.md
output/job-radar/*.json
output/resumes/
output/cover-letters/
output/*.md
# Input personal files
input/old-resumes/
input/*.zip
input/job-postings/
input/raw-notes/
# Zips
*.zip
# Personal config — keep local only, use config.example.py as template
config.py
# Personal profile docs — keep local only, templates are in docs/templates/
docs/*.md
.gstack/
# RAG index cache — derived from personal docs, rebuilt on demand
.rag_cache/
# Personal LinkedIn rewrite + generated images — keep local only
output/linkedin/
# Personal interview prep — keep local only
output/interview-prep/
