# Core
flask>=3.1
requests>=2.31
python-dotenv>=1.0
beautifulsoup4>=4.12
markdown>=3.5

# Document generation
python-docx>=1.1
pypdf>=4.0

# AI providers (all optional except anthropic)
anthropic>=0.40

# Optional — only needed if you use the corresponding provider
# openai>=1.50
# google-genai>=1.0

# Optional — only needed for adzuna_mcp.py
# fastmcp>=2.0
